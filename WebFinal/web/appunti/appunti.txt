
MODIFCARE IL DB PER RENDERE LA DATA UNA STRINGA

ALTER TABLE gabri.psot DROP COLUMN data;
ALTER TABLE gabri.post ADD avatar VARCHAR(50);


ALTER TABLE gabri.file_post DROP COLUMN data;
ALTER TABLE gabri.file_post ADD data VARCHAR(50);

ALTER TABLE gabri.gruppo DROP COLUMN datacreazione;
ALTER TABLE gabri.gruppo ADD datacreazione VARCHAR(50);



		GENERALE
* controllare il giochetto della TIMELINE dei post relativi ai gruppi
* se io invito l apersona nel gruppo non devo inserla nella tabella GRUPPO del DB
ma devo invece aspettare che lei accetti l'invito
* aggiungere filtri (file, gruppi) //FABIO
* aggiungere pagine di errore   //GIANNI
* mettere il controllo su tutte le text edit!
* controllo estensione dei file caricati come immagini
* pagine html di gestione degli errori es. 404,500,...  //GIANNI
* contorllo dimensione di ogni testo(impostare un max numero di caratteri in tutte le textedit)    //GIANNI
* controllo file con lo stesso nome

		GRUPPI
* sistemare front-end nel caso la lista fosse vuota //GIANNI
* includere la lista dei gruppi all'interno di una view scrollabile. Nel caso infatti
    ci fossero troppi gruppi, la pagina viene un po' schifosina //GIANNI

		LISTA INVITI
* sistemare front-end nel caso la lista fosse vuota //GIANNI


		CREAGRUPPO
* invertire i due pulsanti. //GIANNI
* il pulsante HOME NON FUNZIONA SE ALL'INTERNO DEL FORM (stessa cosa anche in GRUPPI)   //GIANNI

		I MIEI DATI
* invertire i due pulsanti.     //GIANNI

                FORUM                
* aggiungere pulsante per tornare alla home //GIANNI
* l'immagine dell'avatar e' sproporzionata  //GIANNI

                ADD POST
* aggiungere un limite in FrontEnd che controlli il numero di caratteri immessi //GIANNI



