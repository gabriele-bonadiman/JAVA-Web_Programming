
MODIFCARE IL DB PER RENDERE LA DATA UNA STRINGA

ALTER TABLE gabri.psot DROP COLUMN data;
ALTER TABLE gabri.post ADD avatar VARCHAR(50);


ALTER TABLE gabri.file_post DROP COLUMN data;
ALTER TABLE gabri.file_post ADD data VARCHAR(50);

ALTER TABLE gabri.gruppo DROP COLUMN datacreazione;
ALTER TABLE gabri.gruppo ADD datacreazione VARCHAR(50);



		GENERALE
* aggiungere filtri
* non funziona la conferma dell'iscrizione ad un gruppo
* aggiungere pagine di errore
* mettere il controllo su tutte le text edit che non siano vuote!
* controllo estensione dei file caricati come immagini e delle dimensioni in generale
* pagine html di gestione degli errori es. 404,500,...
+ aggiungere i controlli che nel nome vengano inseriti solo caratteri normali (i cookie 
    non supportano i caratteri speciali)

		GRUPPI
* sistemare front-end nel caso la lista fosse vuota
* includere la lista dei gruppi all'interno di una view scrollabile. Nel caso infatti
ci fossero troppi gruppi, la pagina viene un po' schifosina

		LISTA INVITI
* sistemare front-end nel caso la lista fosse vuota


		CREAGRUPPO
* invertire i due pulsanti. 
* il pulsante HOME NON FUNZIONA SE ALL'INTERNO DEL FORM (stessa cosa anche in GRUPPI)

		I MIEI DATI
* invertire i due pulsanti. 

                FORUM                
* aggiungere pulsante per tornare alla home
* l'immagine dell'avatar e' sproporzionata
* controllare i vari layout

                ADD POST
* aggiungere un limite in FrontEnd che controlli il numero di caratteri immessi



